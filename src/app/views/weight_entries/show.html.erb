<div class="flex justify-center items-center">
    <div class="bg-white p-8 rounded-xl shadow-lg w-full max-w-md">
        <h1 class="text-2xl font-bold text-gray-800 mb-4 text-center">Détails</h1>
        
        <p class="text-gray-600">Poids : <span class="font-semibold text-gray-800"><%= @weight_entry.weight %></span></p>
        <p class="text-gray-600 mb-4">Pas : <span class="font-semibold text-gray-800"><%= @weight_entry.steps %></span></p>
        <p class="text-gray-600 mb-4">Date : <span class="font-semibold text-gray-800"><%= @weight_entry.date %></span></p>
        
        <div class="flex justify-end">
            <%= link_to 'Retour', weight_entries_path, class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded transition duration-300" %>
        </div>
    </div>
</div>
